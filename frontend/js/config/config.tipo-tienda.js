const TipoTiendaConfig = {
    API: {
        BASE_URL: AppConfig.API.BASE_URL,
        ENDPOINTS: {
            ALL: '/tipoTienda/all',
            CREAR: '/tipoTienda/crear',
            ACTUALIZAR: '/tipoTienda/actualizar',
            ELIMINAR: '/tipoTienda/borrar'
        }
    }
};

window.TipoTiendaConfig = TipoTiendaConfig;
