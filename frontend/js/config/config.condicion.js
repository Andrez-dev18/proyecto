const CondicionConfig = {
    API: {
        BASE_URL: AppConfig.API.BASE_URL,
        ENDPOINTS: {
            ALL: '/condicion/all'
        }
    }
};

window.CondicionConfig = CondicionConfig;
